// import {render, screen} from "@testing-library/react"
import "@testing-library/jest-dom"
// import ActivityItem from "../../../../components/Workout/CreateWorkout/ActivityItem/ActivityItem.jsx"

describe("ActivityItem", () => {
	test("temp", () => {
		expect(true).toBe(true)
	})
	// test("renders textarea with placeholder when input is not disabled", () => {
	// 	const categoryName = "Workout"
	// 	const id = "123"
	// 	const inputDisabled = false
	// 	const text = ""
	// 	render(<ActivityItem categoryName={categoryName} id={id} inputDisabled={inputDisabled} text={text} />)
	// 	const textarea = screen.getByPlaceholderText("Fri text ...")
	// 	expect(textarea).toBeInTheDocument()
	// })

	// test("renders textarea without placeholder when input is disabled", () => {
	// 	const categoryName = "Workout"
	// 	const id = "123"
	// 	const inputDisabled = true
	// 	const text = "This is some text"
	// 	render(<ActivityItem categoryName={categoryName} id={id} inputDisabled={inputDisabled} text={text} />)
	// 	const textarea = screen.getByText(text)
	// 	expect(textarea).toBeInTheDocument()
	// })
})