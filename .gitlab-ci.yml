stages:
  - test
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
test-backend:
  stage: test
  script:
    - cd backend
    - mvn -q test
    
test-linter-frontend:
  stage: test
  image: node:19
  script:
    - cd frontend
    - npm run lint:ci
