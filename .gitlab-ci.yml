stages:
  - linter
  - test
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
test-backend:
  stage: test
  script:
    - cd backend
    - mvn -q test
    
test-linter-frontend:
  stage: linter
  script:
      - cd frontend
      - npm run lint:ci
